# ============================================================
# RTX 4070Ti 최적화 환경 설정 (2026년 1월 1일 기준)
# ============================================================
#
# 이 프로젝트는 두 개의 가상환경을 사용합니다:
#
# 1. venv (LLM 분석용)
#    - PyTorch (CUDA 12.8)
#    - Transformers, FastAPI 등
#    - 설치: setup_venv.bat 또는 requirements_venv.txt 사용
#
# 2. venv_ocr (OCR 학습용)
#    - PaddlePaddle GPU (CUDA 12.x)
#    - PaddleOCR (PP-OCRv5 지원)
#    - 설치: setup_venv_ocr.bat 또는 requirements_venv_ocr.txt 사용
#
# 전체 환경 설정: setup_all.bat 실행
#
# ============================================================

# 이 파일은 참고용입니다. 실제 설치 시 다음 파일을 사용하세요:
# - requirements_venv.txt (LLM 분석용)
# - requirements_venv_ocr.txt (OCR 학습용)
# - setup_venv.bat (venv 자동 설정)
# - setup_venv_ocr.bat (venv_ocr 자동 설정)
# - setup_all.bat (전체 자동 설정)
